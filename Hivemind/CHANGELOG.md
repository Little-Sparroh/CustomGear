# Changelog

## 1.0.0

- Initial release (Phase 0 + Phase 1)
- Registers **Hive Launcher** as a separate primary (gear id `91900`, api `hive_launcher`)
- Clones vanilla **SwarmGun** so hover plant / release dive / tube reload work out of the box
- Vanilla Swarm Launcher left unmodified
- `HiveLauncherBalance` base stats (AMR-style sheet): full-auto, 2 BPS, mag 36 / reserve 288, damage 34
- Equip remap + persistence across save load
- Empty upgrade pool isolated from vanilla Swarm cards (upgrades come in a later phase)
