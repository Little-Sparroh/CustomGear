# Changelog

## 0.1.0

- Phase 0: register **Rapture's Chosen** as a parallel primary (clone Shocklance)
- Unique gear id `87700` / API name `raptures_chosen`
- `RcBalance` single-source base stats (AmrBalance style)
- SpawnGear remap + stamp identity + ApplyUpgrades
- Persistence across save load (`PlayerData.OnAwake` prefix/postfix)
- Phase 1 baseline:
  - M1 charge coil (full charge required; Half Cocked off)
  - Shock element on coil
  - ADS disabled
  - Auger enabled empty-grid on **RMB** (R reloads)
  - Auger drill inherits gun element (including Shock)
- Vanilla Shocklance left untouched
